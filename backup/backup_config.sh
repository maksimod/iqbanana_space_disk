#!/bin/bash

# Сервер, на котором нужно настроить бэкап
BACKUP_SERVER="192.168.0.102"

# IP-адрес SSH хоста (прокси-сервера)
# Будет использоваться, если не указан в .env
DEFAULT_SSH_HOST="46.35.241.37"

# Диск источник, с которого будет браться бэкап (например sdb)
SOURCE_DISK="sdb"
# UUID диска источника (используется вместо имени диска, если указан)
SOURCE_UUID="35ab2ed3-45f7-43c4-b4d4-77bf846dfed1"

# Диск назначения, на который будет сохраняться бэкап
TARGET_DISK="sda"
# UUID диска назначения (используется вместо имени диска, если указан)
TARGET_UUID="9e568244-fae3-49c2-9669-072f43a81f40"

# Частота автоматических бэкапов
# Допустимые значения: hourly, daily, weekly, monthly
BACKUP_FREQUENCY="daily"

# Максимальное количество бэкапов, которое может храниться
# После достижения этого числа самый старый бэкап будет удален
MAX_BACKUPS=24

# Имя бэкап репозитория
REPO_NAME="disk_backup"

# Пароль для шифрования бэкапа (будет использован, если не указан в .env)
# В реальном использовании рекомендуется задать пароль в .env файле
DEFAULT_BORG_PASSPHRASE="change-this-to-a-secure-passphrase"

# Путь монтирования целевого диска
# Если уже используется другая точка монтирования, скрипт запросит подтверждение
# перед отмонтированием и монтированием в новую точку
TARGET_MOUNT="/mnt/backup"

# Путь к лог-файлу
LOG_FILE="$(dirname "$(readlink -f "$0")")/backup.log"

# Путь к пользовательскому скрипту, который будет запущен до бэкапа (если существует)
PRE_BACKUP_SCRIPT=""

# Путь к пользовательскому скрипту, который будет запущен после бэкапа (если существует)
POST_BACKUP_SCRIPT="" 