# NFS exports configuration optimized for large file transfers
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

{% if storage_disks and storage_disks | length > 0 %}
{% for disk in storage_disks %}
{{ nfs_mount_base }}/{{ disk.mountpoint }} {{ nfs_client_ip }}(rw,sync,no_subtree_check,no_root_squash,no_wdelay,no_auth_nlm,insecure,fsid={{ loop.index0 }},anonuid=65534,anongid=65534)
{% endfor %}
{% else %}
# No storage disks found to export
{% endif %}