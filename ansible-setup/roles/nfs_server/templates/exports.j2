# NFS exports configuration
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

# NFS Export Configuration
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

{% for partition in disk_data.blockdevices | selectattr('children', 'defined') | map(attribute='children') | flatten | list %}
{% if partition.mountpoint and partition.mountpoint.startswith(nfs_mount_base) %}
{{ partition.mountpoint }} {{ nfs_client_ip }}(rw,sync,no_subtree_check,no_root_squash)
{% endif %}
{% endfor %}

{% for mount_point in mounted_partitions.stdout_lines %}
{% if mount_point | length > 0 %}
{% set mount_info = mount_point.split() %}
{% set device = mount_info[0] %}
{% set target = mount_info[1] %}
{{ target }} {{ nfs_client_ip | default(hostvars[groups['nfs_client'][0]]['ansible_host']) }}({{ nfs_opts }})
{% endif %}
{% endfor %}